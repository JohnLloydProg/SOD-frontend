export interface User {
    id?: string,
    name?: string,
    email: string,
    password: string,
    contact_number?: string,
    birthday?: Date,
    address?: string,
    created_at?: string,
}
